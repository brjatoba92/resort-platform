export * from './types';
export { localStorage } from './localStorage';
export { sessionStorage } from './sessionStorage';

// Convenience re-exports of commonly used types
export type { StorageOptions, IStorage } from './types';